# Data

The protected benchmark workbook used during validation is intentionally not
included in this repository.

This directory is retained to document the expected pipeline structure.

Users should supply their own source literature and, when performing benchmark
evaluation, their own authorized reference dataset.

The public repository does not contain the protected validation workbook.
